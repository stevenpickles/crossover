//! The Crossover binary: CLI, configuration, and composition root.
//!
//! Wires platform implementations into `crossover-core` behind the
//! `crossover-platform` traits (docs/ARCHITECTURE.md §2, §3). The CLI
//! surface (clap) arrives in a later slice.

fn main() {
    println!("crossover: Phase 0 skeleton — no functionality yet (see docs/ROADMAP.md)");
}
